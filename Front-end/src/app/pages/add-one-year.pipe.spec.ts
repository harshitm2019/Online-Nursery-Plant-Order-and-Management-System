import { AddOneYearPipe } from './add-one-year.pipe';

describe('AddOneYearPipe', () => {
  it('create an instance', () => {
    const pipe = new AddOneYearPipe();
    expect(pipe).toBeTruthy();
  });
});
